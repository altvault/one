[project]
name = "one"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "githubkit>=0.16.0",
    "httpx>=0.28.1",
    "python-debian>=1.1.1",
]

[tool.uv]
exclude-newer = "48 hours"
package = true
